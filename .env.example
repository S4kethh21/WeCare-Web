# WeCare Hospital — Environment Configuration Template
# Copy to .env or set these environment variables in your cloud hosting dashboard (Render / Railway / etc.)

# Database Connection Settings
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=hospital_management

# Optional: Full connection URL (used by Railway, Render, Heroku)
# DATABASE_URL=mysql://root:password@127.0.0.1:3306/hospital_management
